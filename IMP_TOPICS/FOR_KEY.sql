CREATE DATABASE SQL1;

USE SQL1;

-- 1.CREATE TABLES
CREATE TABLE DEPARTMENTS(
DEPT_ID INT PRIMARY KEY,
DEPT_NAME VARCHAR(50)
);

CREATE TABLE STUDENTS(
STUDENT_ID INT PRIMARY KEY ,
NAME VARCHAR(100),
AGE INT,
CITY VARCHAR(150), 
DEPT_ID INT, 
MARKS INT ,
FOREIGN KEY (DEPT_ID) REFERENCES DEPARTMENTS (DEPT_ID)
);

CREATE TABLE COURSES(
COURSES_ID INT PRIMARY KEY,
COURSES_NAME VARCHAR(100),
DEPT_ID INT,
FOREIGN KEY (DEPT_ID) REFERENCES DEPARTMENTS (DEPT_ID)
);

CREATE TABLE ENROLLMENTS(
ENROLL_ID INT PRIMARY KEY,
STUDENT_ID INT,
COURSES_ID INT,
FOREIGN KEY (STUDENT_ID) REFERENCES STUDENTS(STUDENT_ID) , 
FOREIGN KEY (COURSES_ID) REFERENCES  COURSES(COURSES_ID)
);

CREATE TABLE TEACHERS(
TEACHER_ID INT PRIMARY KEY, 
NAME VARCHAR(100), 
DEPT_ID INT,
FOREIGN KEY (DEPT_ID) REFERENCES DEPARTMENTS (DEPT_ID)
);


-- 2.INSERT SAMPLE DATA
INSERT INTO DEPARTMENTS
VALUES
(1 , 'COMPUTER SCIENCE' ),
(2 , 'MATHEMATICS' ),
(3 , 'PHYSICS' ),
(4 , 'BIOLOGY' );

INSERT INTO STUDENTS 
VALUES
(1 , 'JOHN' , 20 , 'DELHI' , 1 , 85) ,
(2 , 'JANE' , 21 , 'MUMBAI' , 1 , 85) ,
(3 , 'MIKE' , 24 , 'RAJKOT' , 2 , 85) ,
(4 , 'SARAH' , 22 , 'SURAT' , 2 , 85) ,
(5 , 'CHRIS' , 23 , 'AHMADABAD' , 3 , 85) ;

INSERT INTO COURSES 
VALUES
(501 , 'DATABASE SYSTEMS' , 1 ),
(502, 'ALGORITHMS', 1),
(503, 'CALCULUS I', 2),
(504, 'QUANTUM MECHANICS', 3);

INSERT INTO ENROLLMENTS
VALUES
(1001, 1, 501) , 
(1002, 2, 502) ,
(1003, 1, 501) ,
(1004, 3, 503) ,
(1005, 4, 504) ,
(1006, 5, 501) ;

INSERT INTO TEACHERS
VALUES
(101, 'DR. ALICE SMITH', 1),
(102, 'PROF. BOB JONES', 2),
(103, 'DR. CAROL WHITE', 1),
(104, 'MR. DAVE BROWN', 3);

-- 1. Show all students.
SELECT * FROM Students;
